 function calculate() {
        const a = Number(prompt('Nhap vao so a'))
    const b = Number(prompt('Nhap vao so b'))
        const total = a + b
        const brand = a - b
        const core = a * b
        const devide = a / b
        console.log({
            total,brand,core,devide,
        })
      }
      calculate()
    

